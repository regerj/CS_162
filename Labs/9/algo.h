

using namespace std;

int * construct_array(int);

int error_handle();

void print_array(int *, int);

int * copy_array(int *, int);

void merge(int * arr, int left_index, int middle, int right_index);

void merge_sort(int * arr, int left_index, int right_index);
